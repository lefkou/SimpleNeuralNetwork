Each folder represent one of the exercises:

The program is written in Java 9.

In order to run the files in each folder do the following:

For exerciseB:

javac QuestionB/*.java && java QuestionB.Main